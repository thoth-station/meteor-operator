
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.4.1
  creationTimestamp: null
  name: meteors.meteor.operate-first.cloud
spec:
  group: meteor.operate-first.cloud
  names:
    kind: Meteor
    listKind: MeteorList
    plural: meteors
    singular: meteor
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Meteor is the Schema for the meteors API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: MeteorSpec defines the desired state of Meteor
            properties:
              ref:
                description: Branch or tag or commit reference within the repository.
                type: string
              ttl:
                description: Time to live after the resource was created. If empty
                  default ttl will be enforced.
                type: integer
              url:
                description: Url points to the source repository.
                type: string
            required:
            - ref
            - ttl
            - url
            type: object
          status:
            description: MeteorStatus defines the observed state of Meteor
            properties:
              conditions:
                description: Current service state of Meteor.
                items:
                  properties:
                    lastProbeTime:
                      description: Last time we probed the condition.
                      format: date-time
                      type: string
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status
                        to another.
                      format: date-time
                      type: string
                    message:
                      description: Human-readable message indicating details about
                        last transition.
                      type: string
                    reason:
                      description: Unique, one-word, CamelCase reason for the condition's
                        last transition.
                      type: string
                    status:
                      description: Status is the status of the condition. Can be Unknown,
                        Ready, Progressing, Failed.
                      type: string
                    type:
                      description: Type is the type of the condition. Currently only
                        JupyterHub, JupyterBook, Deployment.
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
              expireAt:
                description: Once created the expiration clock starts ticking.
                format: date-time
                type: string
              images:
                description: Images built from the source for this Meteor. Empty if
                  no image is built yet.
                items:
                  properties:
                    image:
                      type: string
                    name:
                      type: string
                  required:
                  - image
                  - name
                  type: object
                type: array
              jupyterBook:
                description: JupyterBook host for the Meteor. Routable at least within
                  the cluster. Empty if not yet scheduled.
                type: string
              jupyterHub:
                description: JupyterHub ImageStream name for the Meteor. Empty if
                  not yet created.
                type: string
              message:
                description: A human readable message indicating details about why
                  the Meteor is in this condition.
                type: string
              phase:
                description: Current condition of the Meteor.
                type: string
              reason:
                description: A brief CamelCase message indicating details about why
                  the Meteor is in this state. e.g. 'DiskPressure'
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
